# Impact Analysis

```yaml
RequestID: REQ-20260331-223247
Workflow: 11 (Simulation + Code + Ship)
Profile: r-package
TargetRepoState: empty (greenfield)
```

## Affected Surfaces

### New Files (entire R package scaffold)

**Package metadata:**
- `DESCRIPTION` — package metadata, Rcpp/RcppArmadillo dependencies
- `NAMESPACE` — generated by roxygen2
- `.Rbuildignore`
- `.gitignore`

**C++ source (Rcpp/Armadillo):**
- `src/probit_mle.cpp` — MLE Newton-Raphson estimator
- `src/probit_gibbs.cpp` — Bayesian Gibbs sampler (Albert-Chib)
- `src/probit_mh.cpp` — Metropolis-Hastings MCMC
- `src/utils.cpp` — shared utilities (safe_log_Phi, safe_Phi, rtruncnorm)
- `src/Makevars` — compiler flags for Armadillo

**R wrappers:**
- `R/probit_mle.R` — roxygen docs + export for MLE
- `R/probit_gibbs.R` — roxygen docs + export for Gibbs
- `R/probit_mh.R` — roxygen docs + export for MH
- `R/exampleProbit-package.R` — package-level docs + useDynLib

**Simulation:**
- `inst/simulation/run_simulation.R` — Monte Carlo simulation driver
- `inst/simulation/analyze_results.R` — results analysis + table generation

**Tests:**
- `tests/testthat.R` — test runner
- `tests/testthat/test-probit_mle.R`
- `tests/testthat/test-probit_gibbs.R`
- `tests/testthat/test-probit_mh.R`
- `tests/testthat/test-simulation.R`

**Documentation:**
- `man/` — auto-generated by roxygen2
- `ARCHITECTURE.md` — system architecture (scriber)

## Risk Areas

1. **Numerical stability** — log Phi evaluation for extreme arguments; truncated normal sampling at tails
2. **MCMC convergence** — MH acceptance rate tuning; Gibbs burn-in adequacy
3. **Compilation** — Rcpp/RcppArmadillo linkage on target platform
4. **Simulation runtime** — 500 reps × 4 sample sizes × 3 methods = 6000 fits; needs to complete in reasonable time

## Required Teammates

| Teammate | Role | Write Surface |
|----------|------|---------------|
| planner | Comprehension + spec + test-spec + sim-spec | Run directory artifacts |
| builder | R package scaffold + C++ estimators + R wrappers | Target repo (all package files) |
| tester | Unit tests + R CMD check + simulation validation | Target repo (tests/) + run artifacts |
| simulator | DGP + Monte Carlo harness | Target repo (inst/simulation/) |
| scriber | ARCHITECTURE.md + docs + process log | Target repo (ARCHITECTURE.md) + run artifacts |
| reviewer | Cross-pipeline convergence check | Run artifacts (review.md) |
| shipper | Push to GitHub + workspace sync | Target repo (git) + workspace repo |
